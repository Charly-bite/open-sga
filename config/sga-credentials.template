# SGA Network Share Credentials
# Copy this file to /etc/sga-credentials and set permissions:
# sudo cp sga-credentials.template /etc/sga-credentials
# sudo chmod 600 /etc/sga-credentials

username=sga
password=YOUR_PASSWORD_HERE
domain=WORKGROUP
