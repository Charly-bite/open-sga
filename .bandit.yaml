# Bandit config file
skips:
  - B101 # assert_used
  - B110 # try_except_pass
  - B311 # random
  - B404 # import_subprocess
  - B104 # hardcoded_bind_all_interfaces (dev server binds 0.0.0.0 intentionally)
  - B201 # flask_debug_true (only in __main__ dev entry point)
  - B602 # subprocess_popen_with_shell_equals_true (net use commands require shell=True)
  - B608 # hardcoded_sql_expressions (table names from internal config, not user input)
  - B310 # urllib_urlopen (internal health-check URL, not user-controlled)
